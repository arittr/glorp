# Glorp Companion Scene Runtime Baseline

Generated by `cargo xtask companion scene-baseline` from a release `retained-renderer` build. The fixture uses an isolated Glorp config, deterministic seed, redacted HUD, and a 360x360 logical window. The first 20 visible ticks are discarded before steady-state sampling.

## Build and host identity

- Git commit: `ea0df01f49f64844bb5883feb0243e4cbc9227fc`
- Rust: `rustc 1.97.0 (2d8144b78 2026-07-07)`
- macOS: `26.5.2`
- Architecture: `arm64`
- Hardware model: `Mac17,9`
- Build: `release`, feature `retained-renderer`
- Requested duration: 120000 ms
- Steady visible samples: 1779

## Measured baseline

| Metric | p50 (us) | p95 (us) | p99 (us) |
|---|---:|---:|---:|
| UI tick | 767 | 898 | 1034 |
| Frame preparation | 52 | 152 | 170 |
| Encode | 22 | 31 | 38 |
| Queue submit wait | 306 | 363 | 448 |
| AppKit raster/compile slice | 13494 | 13494 | 13494 |
| Activation render-owner slice | 69 | 69 | 69 |

Post-warmup persistent GPU creations: 0.  Post-warmup static upload bytes: 0.  CPU accounted-byte high-water: 147456.  GPU accounted-byte high-water: 6553600.  Hidden ticks observed in the visible baseline fixture: 0.

## Frozen gates

- UI tick p95 <= `1398 us` (`min(8000us, max(baseline p95 * 1.10, baseline p95 + 500us))`).
- UI tick p99 <= `2034 us` (`min(16000us, max(baseline p99 * 1.15, baseline p99 + 1000us))`).
- Encode p95 <= `281 us` (`max(baseline p95 * 1.10, baseline p95 + 250us)`).
- AppKit raster slice <= `4000 us`; measured p95 `13494 us`.
- Activation render-owner slice <= `16000 us`; measured p95 `69 us`.
- Metrics overhead <= `2%` of baseline UI-tick p95 (maximum `17.96 us`); measured high-water `10 us`.
- Hidden steady state after one transition tick = zero prepare/write/acquire/encode/submit.
- Ordinary post-warmup persistent GPU creations = `0`; measured `0`.
- Ordinary post-warmup static upload bytes = `0`; measured `0`.
- RSS and accounted GPU bytes after 4500 virtual frames <= warmup high-water + `1%`.

## Current baseline concern

**FAIL:** the one-time AppKit raster/compile slice measured `13494 us`, exceeding the frozen `4000 us` gate. The command preserves this miss as stop-gate evidence; it does not loosen or derive the absolute gate from the failing baseline.

## Capacity inventory

| Capacity | Frozen maximum |
|---|---:|
| Nodes | 128 |
| Static primitives | 768 |
| Pet art slots | 130 |
| Visible props | 10 |
| Round tank inhabitants | 2 |
| Ambient instances | 64 |
| Blended draw records | 256 |
| Lights | 2 |
| Attachments | 32 |

Every inventory value is at or below the versioned Global Constraints limit.
